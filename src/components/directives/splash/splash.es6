app.component('splashItem', {
    templateUrl: 'splash.html',
    controllerAs: 'splash',
    bindings: {
        img: '@',
        heading: '@'
    },
    controller: function ($element, $timeout) {

        var init = () => {};

        init();

        _.extend(this, {});
    }
});
